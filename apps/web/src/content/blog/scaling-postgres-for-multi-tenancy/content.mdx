This post is still a draft, and is here to prove that drafts stay invisible: it
is registered exactly like a published post, yet it does not appear in the blog
index, the sitemap or the docs search, and its URL answers 404 in production.

## Shared schema, filtered rows

The default: every business table carries an owner column, and the data-access
layer scopes every query by it.

## Row-level security

The second line of defence, enforced by the database rather than by application
code.

## When to partition

Notes to follow.
