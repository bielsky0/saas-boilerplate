/**
 * Optional plugins feature module (spec 18).
 *
 * Self-contained add-on features: testimonials (with moderation queue),
 * in-app feedback widget, roadmap, wishlist/feature voting, waitlist, and
 * contact form. Each plugin should be independently removable without breaking
 * the core app. Add one subfolder per plugin as it is implemented.
 */

export {};
