/**
 * Back-compat shim — the source of truth moved to `@repo/contracts/auth`.
 * The Better Auth implementation stays beside this file until it moves to Nest.
 */
export * from "@repo/contracts/auth";
