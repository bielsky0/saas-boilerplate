/**
 * Back-compat shim — the source of truth moved to `@repo/i18n-core`.
 * React/Next bindings stay here (`./navigation`, `./request`, …); pure
 * locale vocabulary (LOCALES, Locale, path helpers, negotiation) is shared.
 */
export * from "@repo/i18n-core";
