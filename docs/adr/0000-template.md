# ADR-NNNN: Title

- **Status:** proposed | accepted | superseded by ADR-MMMM
- **Date:** YYYY-MM-DD
- **Context:** product code (boilerplate for developers) or production SaaS?

## Context

What problem are we solving, and what constraints bind the solution?
Link the spec section (`docs/specyfikacja.md`) and the phase (`docs/etap-2.md`)
when applicable.

## Decision

What we decided, in one paragraph. Name the files/modules that carry it.

## Consequences

What gets better, what gets harder, what must never be "fixed" away.

## Alternatives considered

What we rejected, and why — so the next reader does not re-litigate it.
