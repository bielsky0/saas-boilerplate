/**
 * Storage provider adapter (spec 1.2 — pluggable file storage).
 *
 * Defines the internal contract for uploading, retrieving, and generating
 * signed URLs for files (e.g. organization logos, user avatars). Concrete
 * implementation wraps the chosen object-storage provider and is swappable
 * without changing feature code.
 */

export {};
