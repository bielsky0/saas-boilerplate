/**
 * Cross-cutting library barrel (spec 17.2).
 *
 * Shared, framework-level utilities that are not tied to a single feature:
 * validation helpers, formatting, logging, constants, etc. Provider-specific
 * code belongs in `./adapters`; database in `./db`; environment in `./env`.
 */

export {};
