/**
 * Content / CMS feature module (spec 8 & 9 — blog, docs, changelog, SEO).
 *
 * Blog posts, documentation/help center, and changelog built from a single
 * content mechanism, statically generated under predictable URLs. Owns
 * per-page metadata, Open Graph/Twitter cards, JSON-LD structured data, and
 * sitemap/robots generation for public pages.
 */

export {};
