/**
 * Design-system primitives (spec 7.1 — headless, shadcn/ui-style components).
 *
 * Low-level, unopinionated UI primitives driven by centralized design tokens so
 * a theme change means editing tokens, not every component. Populated when the
 * design-system module (spec 7) is built.
 */

export {};
